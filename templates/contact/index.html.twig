{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
<h1>Faites nous part de votre question</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
